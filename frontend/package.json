{
  "name": "robot-arena",
  "author": "NguyenHuuHung",
  "private": true,
  "packageManager": "pnpm@9.15.5",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter \"./packages/*\" build && pnpm --filter web build",
    "build:packages": "pnpm --filter \"./packages/*\" build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
